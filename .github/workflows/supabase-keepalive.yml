name: Supabase Keep Alive

on:
  schedule:
    # Runs every 5 days at midnight
    - cron: '0 0 */5 * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase
        run: |
          curl -X GET "https://stayinubud.netlify.app/api/keep-alive"